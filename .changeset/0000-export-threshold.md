---
"evalite": minor
---

Added `--threshold` flag to `evalite export` command. When specified, the export process exits with code 1 if scores are below the threshold, matching the behavior of `evalite run`.
